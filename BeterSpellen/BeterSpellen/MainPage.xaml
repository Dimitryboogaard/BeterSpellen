<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BeterSpellen.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <ListView x:Name="listView"
              Margin="20"
              ItemSelected="OnListViewItemSelected"
              HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label x:Name="Antwoord1" Text="{Binding Antwoord1}" />
                            <Label x:Name="Antwoord2" Text="{Binding Antwoord2}" />
                            <Label x:Name="Antwoord3" Text="{Binding Antwoord3}" />
                            <Label x:Name="Antwoord4" Text="{Binding Antwoord4}" />

                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
